# Backend environment.
# Locally, copy to .env. On Render these are set as service env vars by the
# deploy (the database URL + any external-service keys you provided).
MONGODB_URI=
DB_NAME=
PORT=8080
# Auth (only if the app has accounts): a JWT signing secret + SendGrid email.
JWT_SECRET=
EMAIL_API_KEY=
EMAIL_FROM=
